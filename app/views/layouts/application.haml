%html
  %head
    %title= @title ? @title : 'ParlyTags'
    = stylesheet_link_tag "screen"
    -if @map
      = GMap.header
      = @map.to_html
  %body
    #header
      %h1= link_to_unless_current "PARLYTAGS &mdash; Find places in the UK mentioned in Parliament", root_path
      #search
        - form_tag(search_url, :name => 'search', :method => :get) do
          %label{:for=>"q"}
            Try Leith, Bolton, Cardiff &hellip;
          = text_field_tag 'q', (@last_search_term ? h(sanitize(@last_search_term)) : ''), :accesskey => 's'
          = submit_tag 'Search'
    #content
      = yield      
    #footer
      = yield :footer
      
    
  
