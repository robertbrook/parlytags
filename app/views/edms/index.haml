
- @edms.each do |edm|
  .edm
    %h2= edm.title
    .number="EDM #{edm.number}"
    .sponsor= "#{edm.proposers.first.member.display_name} and " + pluralize(edm.signature_count-1, "other")
    %blockquote= edm.text
  
#pagination= will_paginate @edms
