require File.dirname(__FILE__) + '/../spec_helper'

describe MembersController do

  it "should return a paginated list of Members when called with 'index', ordered by name"

  it "should return one Member when called with 'show'"

  it "should return something when called with 'proposed' but I'm not sure what"
  
  it "should return something when called with 'seconded' but I'm not sure what"
  
  it "should return something when called with 'signed' but I'm not sure what"

end